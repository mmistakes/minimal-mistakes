1. install rbenv
2. install ruby 3.1.3 with `rbenv install 3.1.3`
3. install gems with `bundle install`
4. install jekyll with `gem install jekyll`
4. run server with `bundle exec jekyll serve --watch`