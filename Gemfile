source "https://rubygems.org"

# Theme gem
gem "minimal-mistakes-jekyll", path: "." # Use the path to your theme's root directory

# GitHub Pages dependencies
gem "github-pages", group: :jekyll_plugins