'''
EX. 11 (1 point)
Code a function `is_number(string)`, which will check if the provided `string` is a number. 
Your aim is to write a function with the aid of regular expressions, which is to check if there 
are `+` or `-` singe at the beginning, any digit, and if there is a `.` inside the string.    

'''

###START FROM HERE

import re

def is_number(string):


    return