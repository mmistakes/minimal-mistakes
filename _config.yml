# Jekyll 설정
markdown: kramdown
highlighter: rouge
lsi: false
excerpt_separator: "\n\n"
incremental: false
encoding: "utf-8"
markdown_ext: "markdown,mkdown,mkdn,mkd,md"

# Minimal Mistakes 테마 설정
minimal_mistakes_skin: "default" # 원하는 스킨으로 변경 가능
# theme: "minimal-mistakes-jekyll"
# remote_theme: "mmistakes/minimal-mistakes"

# 🛑 사이트 필수 설정 (반드시 수정하세요!) 🛑
title: "Orange Tech blogs"
title_separator: "-"
# subtitle: "사이트를 설명하는 짧은 태그라인"
url: "https://o5l3.github.io" # 사용자 계정 도메인으로 설정 (o5l3.github.io)
baseurl: "/OrangeLabs_blog" # 저장소 이름과 동일하게 설정 (o5l3.github.io/OrangeLabs_blog)
repository: "o5l3/OrangeLabs_blog" # GitHub 사용자명/저장소 이름

# SEO Related
# google_site_verification:
naver_site_verification: # 네이버 검색 등록 시 필요

# Analytics
analytics:
  provider: # "google-gtag", "google-universal" 등 선택
  google:
    tracking_id: # 구글 애널리틱스 ID

# Site Footer
footer:
  links:
    - label: "Notion"
      icon: "fab fa-fw fa-twitter-notion"
      url: "https://o5l3.notion.site/"
  since: "2023"

# Outputting
permalink: /:categories/:title/
timezone: "Asia/Seoul" # 시간대 설정 (선택 사항)

# Plugins
plugins:
  - jekyll-paginate
  - jekyll-sitemap
  - jekyll-gist
  - jekyll-feed
  - jekyll-include-cache

# Defaults (포스트에 기본으로 적용되는 레이아웃)
defaults:
  - scope:
      path: ""
      type: posts
    values:
      layout: single
      author_profile: true
      read_time: true
      comments: true # 댓글 기능 활성화 여부
      share: true
      related: true

# Reading Files
include:
  - .htaccess
  - _pages
# 필요한 파일만 남기고 나머지 Exclude 항목은 모두 제거됨
exclude:
  - vendor
  - Gemfile
  - Gemfile.lock
  - node_modules
  - package.json
  - Rakefile
  - README.md
