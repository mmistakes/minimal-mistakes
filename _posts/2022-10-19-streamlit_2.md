---
layout: single
title:  "Streamlit 앱 만들기"
categories : streamlit
tag : [streamlit, machine-learning, python, web]
toc: true
toc_sticky: true

---

![header](https://capsule-render.vercel.app/api?type=waving&color=a2dcec&height=300&section=header&text=Streamlit 앱 만들기&fontSize=40&animation=fadeIn&fontAlignY=38&fontColor=FFFFFF)

- 참고 : [Streamlit 공식사이트](https://streamlit.io/)

------------------------------------------------------

&nbsp;

## streamlit 앱 만들기

&nbsp;

### Data 불러오기 및 로딩

```python
import streamlit as st
import pandas as pd
import numpy as np

st.title('연습용 앱 생성')

DATE_COLUMN = 'date/time'
DATA_URL = ('https://s3-us-west-2.amazonaws.com/'
         'streamlit-demo-data/uber-raw-data-sep14.csv.gz')

def load_data(nrows):
    data = pd.read_csv(DATA_URL, nrows=nrows)
    lowercase = lambda x: str(x).lower()
    data.rename(lowercase, axis='columns', inplace=True)
    data[DATE_COLUMN] = pd.to_datetime(data[DATE_COLUMN])
    return data


data_load_state = st.text('데이터 로딩...')
data = load_data(10000)
st.dataframe(data)
data_load_state.text('데이터 로딩...완!')
```

![image-20221019104446574](/images/2022-10-19-streamlit_2/image-20221019104446574.png)

- **st.cache**

```python
import streamlit as st
import pandas as pd
import numpy as np

st.title('연습용 앱 생성')

DATE_COLUMN = 'date/time'
DATA_URL = ('https://s3-us-west-2.amazonaws.com/'
         'streamlit-demo-data/uber-raw-data-sep14.csv.gz')

@st.cache
def load_data(nrows):
    data = pd.read_csv(DATA_URL, nrows=nrows)
    lowercase = lambda x: str(x).lower()
    data.rename(lowercase, axis='columns', inplace=True)
    data[DATE_COLUMN] = pd.to_datetime(data[DATE_COLUMN])
    return data


data_load_state = st.text('데이터 로딩...')
data = load_data(10000)
st.dataframe(data)
data_load_state.text('데이터 로딩...완!')
```

- st.cache를 사용하면 한번 loading 한 데이터 빠르게 다시 loading 가능

&nbsp;

### 데이터 출력 및 히스토그램 그리기

```python
import streamlit as st
import pandas as pd
import numpy as np

st.title('연습용 앱 생성')

DATE_COLUMN = 'date/time'
DATA_URL = ('https://s3-us-west-2.amazonaws.com/'
         'streamlit-demo-data/uber-raw-data-sep14.csv.gz')

@st.cache
def load_data(nrows):
    data = pd.read_csv(DATA_URL, nrows=nrows)
    lowercase = lambda x: str(x).lower()
    data.rename(lowercase, axis='columns', inplace=True)
    data[DATE_COLUMN] = pd.to_datetime(data[DATE_COLUMN])
    return data


data_load_state = st.text('데이터 로딩...')
data = load_data(10000)
data_load_state.text('데이터 로딩...완!')

st.subheader('데이터 확인')
st.write(data)
```

![image-20221019105314074](/images/2022-10-19-streamlit_2/image-20221019105314074.png)

- **히스토그램 그리기**

```python
st.subheader('히스토그램 그리기')
hist_values = np.histogram(
    data[DATE_COLUMN].dt.hour, bins=24, range=(0,24))[0]
st.bar_chart(hist_values)
```

![image-20221019105559511](/images/2022-10-19-streamlit_2/image-20221019105559511.png)

- **지도 표시**

```python
st.subheader('지도 표시')
hour_to_filter = 17
filtered_data = data[data[DATE_COLUMN].dt.hour == hour_to_filter]
st.subheader(f'지도 시간 {hour_to_filter}:00')
st.map(filtered_data)
```

![image-20221019111332890](/images/2022-10-19-streamlit_2/image-20221019111332890.png)

&nbsp;

### 슬라이더로 결과 필터링

```python
st.subheader('지도 표시')
hour_to_filter = st.slider('시간', 0, 23, 17)
filtered_data = data[data[DATE_COLUMN].dt.hour == hour_to_filter]
st.subheader(f'지도 시간 {hour_to_filter}:00')
st.map(filtered_data)
```

![image-20221019112507127](/images/2022-10-19-streamlit_2/image-20221019112507127.png)

&nbsp;

### 데이터 on/off

```python
if st.checkbox('data show on/off'):
    st.subheader('데이터 표시')
    st.write(data)
```

![image-20221019112900927](/images/2022-10-19-streamlit_2/image-20221019112900927.png)

&nbsp;

## 전체 과정

```python
import streamlit as st
import pandas as pd
import numpy as np

st.title('Uber pickups in NYC')

DATE_COLUMN = 'date/time'
DATA_URL = ('https://s3-us-west-2.amazonaws.com/'
            'streamlit-demo-data/uber-raw-data-sep14.csv.gz')

@st.cache
def load_data(nrows):
    data = pd.read_csv(DATA_URL, nrows=nrows)
    lowercase = lambda x: str(x).lower()
    data.rename(lowercase, axis='columns', inplace=True)
    data[DATE_COLUMN] = pd.to_datetime(data[DATE_COLUMN])
    return data

data_load_state = st.text('Loading data...')
data = load_data(10000)
data_load_state.text("Done! (using st.cache)")

if st.checkbox('Show raw data'):
    st.subheader('Raw data')
    st.write(data)

st.subheader('Number of pickups by hour')
hist_values = np.histogram(data[DATE_COLUMN].dt.hour, bins=24, range=(0,24))[0]
st.bar_chart(hist_values)

# Some number in the range 0-23
hour_to_filter = st.slider('hour', 0, 23, 17)
filtered_data = data[data[DATE_COLUMN].dt.hour == hour_to_filter]

st.subheader('Map of all pickups at %s:00' % hour_to_filter)
st.map(filtered_data)
```

- [전체과정 확인 링크](https://choijhyeok-streamlit-example-app-6ekapk.streamlitapp.com/)