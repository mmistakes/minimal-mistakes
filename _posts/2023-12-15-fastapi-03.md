---
layout: single
title: "FastAPI를 이용 하여 AI 서비스 하기 - 03 (FastAPI: Query Parameters)"
categories: FastAPI
tag: [FastAPI, uvicorn, Query Parameters]
toc: true
toc_sticky: true
toc_label: "목차"
---
## Query Parameters

출처: [https://fastapi.tiangolo.com/tutorial/query-params/](https://fastapi.tiangolo.com/tutorial/query-params/)  

### Query Parameters 예시

경로 매개변수가 아닌 변수가 함수 매개변수로 선언되면 자동으로 Query Parameter로 처리 됩니다.  

```python
from fastapi import FastAPI

app = FastAPI()

fake_items_db = [{"item_name": "Foo"}, {"item_name": "Bar"}, {"item_name": "Baz"}]


@app.get("/items/")     # 경로 매개변수가 없음
async def read_item(skip: int = 0, limit: int = 10): # skip, limit는 Query Parameter
    return fake_items_db[skip : skip + limit]
```

skip, limit는 경로 Parameter에 선언 되지 않았으므로 Query Parameter로 처리 됩니다.  

```text
http://127.0.0.1:8000/items/?skip=0&limit=10
```

url의 `?` 뒤 부분이 `key=value`를 나타내고 `&`는 구분자 입니다..  
`skip`은 0, `limit`는 10의 값인 Query Parameter가 됩니다.

```json
{"item_id":"foo"}
```

## TODO

해시태그: #fastapi #uvicorn #query parameters #port
