---
layout: single
title: "[백준 알고리듬] 단계별로 풀어보기 > if문 > 2753 윤년"
tags:
  - [알고리듬, 백준, 자바스크립트, Javascript, Node.js]
---

<https://www.acmicpc.net/problem/2753>

```javascript
const fs = require('fs');
const filePath = process.platform === 'linux' ? '/dev/stdin' : './input.txt';
const input = fs.readFileSync(filePath).toString().split(' ').map(item => +item);

function solution(year) {
    if ((year % 4 === 0 && year % 100 !== 0) || year % 400 === 0) {
        console.log(1);
    } else {
        console.log(0);
    }
};

solution(input);
```
<br>
<br>
<br>
아래처럼도 가능하다 (else구문 대신에 return을 사용)

```javascript
const fs = require('fs');
const filePath = process.platform === 'linux' ? '/dev/stdin' : './input.txt';
const input = fs.readFileSync(filePath).toString().split(' ').map(item => +item);

function solution(year) {
    if ((year % 4 === 0 && year % 100 !== 0) || year % 400 === 0) {
        console.log(1);
        return;
    } 
    console.log(0);
};

solution(input);
```
